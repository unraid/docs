import { IconInfoCircle } from "@tabler/icons-react";
import { ThemeIcon, type ThemeIconProps } from "../../Icon/shared";

export default function AdmonitionIconInfo(props: ThemeIconProps) {
  return <ThemeIcon icon={IconInfoCircle} {...props} />;
}
