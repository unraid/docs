import { IconFileDescriptionFilled } from "@tabler/icons-react";
import { ThemeIcon, type ThemeIconProps } from "../../Icon/shared";

export default function AdmonitionIconNote(props: ThemeIconProps) {
  return <ThemeIcon icon={IconFileDescriptionFilled} {...props} />;
}
