import { IconFlame } from "@tabler/icons-react";
import { ThemeIcon, type ThemeIconProps } from "../../Icon/shared";

export default function AdmonitionIconDanger(props: ThemeIconProps) {
  return <ThemeIcon icon={IconFlame} {...props} />;
}
