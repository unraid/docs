import { IconBulb } from "@tabler/icons-react";
import { ThemeIcon, type ThemeIconProps } from "../../Icon/shared";

export default function AdmonitionIconTip(props: ThemeIconProps) {
  return <ThemeIcon icon={IconBulb} {...props} />;
}
