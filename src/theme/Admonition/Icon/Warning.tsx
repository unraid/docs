import { IconAlertSquareRoundedFilled } from "@tabler/icons-react";
import { ThemeIcon, type ThemeIconProps } from "../../Icon/shared";

export default function AdmonitionIconWarning(props: ThemeIconProps) {
  return <ThemeIcon icon={IconAlertSquareRoundedFilled} {...props} />;
}
