import { IconTextWrap } from "@tabler/icons-react";
import { ThemeIcon, type ThemeIconProps } from "../shared";

export default function IconWordWrap(props: ThemeIconProps) {
  return <ThemeIcon icon={IconTextWrap} {...props} />;
}
