import { IconEdit as TablerIconEdit } from "@tabler/icons-react";
import { ThemeIcon, type ThemeIconProps } from "../shared";

export default function IconEdit(props: ThemeIconProps) {
  return <ThemeIcon icon={TablerIconEdit} defaultWidth={20} defaultHeight={20} {...props} />;
}
