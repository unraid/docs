import { IconDeviceDesktop } from "@tabler/icons-react";
import { ThemeIcon, type ThemeIconProps } from "../shared";

export default function IconSystemColorMode(props: ThemeIconProps) {
  return <ThemeIcon icon={IconDeviceDesktop} defaultWidth={20} defaultHeight={20} {...props} />;
}
