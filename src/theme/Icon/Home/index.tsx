import { IconHome as TablerIconHome } from "@tabler/icons-react";
import { ThemeIcon, type ThemeIconProps } from "../shared";

export default function IconHome(props: ThemeIconProps) {
  return <ThemeIcon icon={TablerIconHome} {...props} />;
}
