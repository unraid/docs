import { IconCopy as TablerIconCopy } from "@tabler/icons-react";
import { ThemeIcon, type ThemeIconProps } from "../shared";

export default function IconCopy(props: ThemeIconProps) {
  return <ThemeIcon icon={TablerIconCopy} {...props} />;
}
