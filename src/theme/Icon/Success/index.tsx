import { IconCheck } from "@tabler/icons-react";
import { ThemeIcon, type ThemeIconProps } from "../shared";

export default function IconSuccess(props: ThemeIconProps) {
  return <ThemeIcon icon={IconCheck} {...props} />;
}
