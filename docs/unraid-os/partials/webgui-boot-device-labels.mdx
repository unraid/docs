
:::note[WebGUI: Boot device vs Flash]

In Unraid 7.3.0 and later, the %%WebGUI|web-gui%% uses **Boot device** on the **Main** tab (for example **Boot Device** settings and backups). On earlier releases, the same controls are labeled **Flash** or **Flash device**. Use whichever label your version shows.

:::
