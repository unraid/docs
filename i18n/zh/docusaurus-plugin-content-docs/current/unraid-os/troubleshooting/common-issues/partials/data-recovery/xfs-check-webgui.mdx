1. 使用 %%WebGUI|web-gui%% 对于格式化为 %%XFS|xfs%% 的 %%array|array%% 驱动器。
2. 以 %%Maintenance Mode|maintenance-mode%%（未挂载）启动 %%array|array%%。
3. 从主选项卡中选择磁盘并打开 **检查文件系统状态**。
4. 默认情况下，检查使用 `-n`（非修改）；想要更多输出，请在 `nv` 上添加 `-v`。
5. 要修复，请删除 `-n` 标志以允许修复。

%%WebGUI|web-gui%% 过程在修复期间保持 %%parity|parity%%。
