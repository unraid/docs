1. 如上所述**禁用 Docker 和 %%VM|vm%% 服务**。
2. 使用 `rsync` 或 `mv` 命令在挂载点之间传输文件（例如，`/mnt/pool1/share` 到 `/mnt/pool2/share`）。
3. 在从原始存储池中删除文件之前，请核实这些文件。
4. 重新启用 Docker 和 %%VM|vm%% 服务。

:::caution

Always check your file paths and use the correct share names to prevent data loss.

:::
