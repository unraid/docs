**%%XFS|xfs%%** 是 Unraid 中 %%array|array%% 驱动器的默认文件系统。它以其稳健性和在意外关机或崩溃后恢复损坏文件系统的能力而闻名。每个用 %%XFS|xfs%% 格式化的驱动器都作为一个独立的文件系统运行，这使得在任何 Linux 系统上的数据恢复都很简单。

- **数据保护：** 没有内建冗余；依赖 Unraid 的 %%parity|parity%% 进行保护。
- \*\*性能：\*\*一致且可靠。
- \*\*最适合：\*\*特别是当稳定性和数据恢复易用性优先时的%%Array|array%%驱动器。
