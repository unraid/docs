此方法将清除**所有用户密码**，包括\*%%root|root-user%% 和共享用户。

要重置密码的最简单方法：

1. **关闭** 您的 Unraid 服务器。
2. **将 USB 启动设备连接**到电脑（Windows 或 Mac）。
3. **删除这些文件** 从 USB 驱动器中：
   - `/config/shadow`
   - `/config/smbpasswd`
4. **断开 USB 启动设备** 并将其重新连接到您的 Unraid 服务器，然后启动它。
5. **在启动时创建新 root 密码**。
