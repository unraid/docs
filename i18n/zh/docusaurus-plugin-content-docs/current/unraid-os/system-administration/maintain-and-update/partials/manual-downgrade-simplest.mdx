1. 从 [版本历史](../../../download_list.mdx) 下载所需的 Unraid 版本 ZIP 文件。
2. 在电脑上解压文件。
3. 访问 `flash` 共享或者将 USB 闪存设备连接到电脑。
4. 如果不存在，请创建一个 `previous` 目录。
5. 将所有 `bz*` 和 `changes.txt` 文件移到 `previous` 目录中。
6. 将新 `bz*` 和 `changes.txt` 文件复制到闪存驱动器根目录。
7. 重新启动您的服务器。
