1. Utilisez le %%WebGUI|web-gui%% pour les disques %%array|array%% formatés en %%XFS|xfs%%.
2. Démarrez le %%array|array%% en %%Maintenance Mode|maintenance-mode%% (non monté).
3. Depuis l'onglet Principal, sélectionnez le disque et ouvrez **Vérifier l'état du système de fichiers**.
4. Par défaut, une vérification utilise `-n` (non modifiant); pour plus de sorties, ajoutez `-v` pour `-nv`.
5. Pour réparer, retirez le drapeau `-n` pour permettre les corrections.

Le processus %%WebGUI|web-gui%% maintient la protection %%parity|parity%% pendant la réparation.
