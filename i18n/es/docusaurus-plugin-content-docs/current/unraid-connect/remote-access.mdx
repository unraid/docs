---
sidebar_position: 3
sidebar_label: Acceso remoto
---

# Acceso remoto

Esta página cubre **Unraid Connect Remote Access**, que publica tu %%WebGUI|web-gui%% en Internet mediante %%UPnP|upnp%% o una regla manual de reenvío de puertos en tu router. Si deseas una administración remota completa sin exponer el %%WebGUI|web-gui%% al tráfico WAN, es preferible la [integración oficial de %%Tailscale|tailscale%%](../unraid-os/system-administration/secure-your-server/tailscale.mdx). Para necesidades más avanzadas, como conectarte a contenedores Docker o acceder a unidades de red, un %%VPN Tunnel|vpn-tunnel%% sigue siendo la solución recomendada.

:::important[Security recordatorio]

El acceso remoto a través de Unraid Connect proporciona:

:::

:::note[Connect el acceso al panel es diferente]

Iniciar sesión en el [panel de Unraid Connect](https://connect.myunraid.net/) **no** expone por sí mismo tu %%WebGUI|web-gui%% a Internet. El acceso al panel usa una conexión iniciada por el servidor con acceso restringido a la API para las funciones compatibles de Connect. **Unraid Connect Remote Access** es una función अलग?

:::

El acceso remoto a través de Unraid Connect proporciona:

- **Comodidad** - Accede rápidamente a la interfaz de gestión de tu servidor desde cualquier lugar, usando una URL confiable de Unraid Connect.
- **Seguridad** - El tráfico de tu %%WebGUI|web-gui%% se cifra cuando SSL/TLS está configurado correctamente.
- **Simplicidad** - %%UPnP|upnp%% puede crear automáticamente el mapeo de puertos requerido cuando tu router lo admite.

:::tip[Preferred acercamiento]

Para la mayoría de los usuarios, la [integración oficial de %%Tailscale|tailscale%%](../unraid-os/system-administration/secure-your-server/tailscale.mdx) es la mejor opción para la administración remota porque evita exponer el %%WebGUI|web-gui%% a Internet pública. Usa Unraid Connect Remote Access solo cuando necesites específicamente acceso desde el navegador a la URL pública del %%WebGUI|web-gui%%.

:::

---

## Configuración inicial

Tu servidor Unraid estará listo para aceptar conexiones remotas seguras a través del %%WebGUI|web-gui%%, usando el puerto configurado y un certificado confiable.

1. En el Unraid %%WebGUI|web-gui%%, navega a ***Configuraciones → Acceso de Gestión***.
2. Verifique el **puerto HTTPS** (por defecto: 443). Si este puerto está en uso (por ejemplo, por Docker), seleccione un puerto no utilizado por encima de 1000 (como 3443, 4443 o 5443).
3. Haz clic en **Aplicar** si cambiaste alguna configuración.
4. Debajo de **archivo de certificado firmado por CA**, haz clic en **Proveer** para generar un certificado confiable.

Tu servidor Unraid estará listo para aceptar conexiones remotas seguras a través del %%WebGUI|web-gui%%, usando el puerto configurado y un certificado confiable.

---

## Cómo elegir una configuración de acceso remoto

En las versiones actuales de Unraid 7.2.x, la página de configuración de la API de Unraid expone estos controles de acceso remoto:

- **Permitir acceso remoto** - Elige si el acceso remoto está **Deshabilitado** o siempre (**Always**) disponible.
- **Tipo de reenvío de acceso remoto** - Elige **Static** para el reenvío manual de puertos, o **UPnP** para que tu router cree automáticamente el mapeo de puertos.
- **Puerto WAN de acceso remoto** - Elige el puerto WAN que se reenviará cuando uses el reenvío **Static**.

When remote access is set to **Always**, your %%WebGUI|web-gui%% is continuously reachable from the internet on the configured WAN port. Both **Static** and **UPnP** remote access publish the %%WebGUI|web-gui%% to WAN traffic. Use a strong root password, keep Unraid OS updated, and disable remote access when you do not need it.

El acceso remoto dinámico bajo demanda no está expuesto actualmente en la página de configuración de la API de Unraid. Si necesitas administración remota sin mantener el %%WebGUI|web-gui%% expuesto mediante reenvío de puertos, preferir [%%Tailscale|tailscale%%](../unraid-os/system-administration/secure-your-server/tailscale.mdx) o otro %%VPN Tunnel|vpn-tunnel%%.

## Habilitar acceso remoto

Tu servidor Unraid estará listo para aceptar conexiones remotas seguras a través del %%WebGUI|web-gui%%, usando el puerto configurado y un certificado confiable.

1. En el %%WebGUI|web-gui%% de Unraid, ve a ***Settings → Management Access*** y abre la pestaña **Unraid API Settings**.
2. Configura **Allow Remote Access** en **Always**.
3. Elige un **Remote Access Forward Type**:
   - **UPnP:** permite que Unraid pida a tu router que cree automáticamente el mapeo del puerto WAN. Esto requiere compatibilidad con %%UPnP|upnp%% y que %%UPnP|upnp%% esté habilitado en tu router.
   - **Static:** requiere que crees manualmente la regla de reenvío de puertos en tu router.
4. Si eliges **Static**, introduce el **Remote Access WAN Port** que quieres usar.
5. Haga clic en **Aplicar**.
6. Inicia sesión en [Unraid Connect](https://connect.myunraid.net/) y haz clic en el enlace **Manage** para conectarte a tu servidor de forma remota.

Para configurar %%UPnP|upnp%%:

---

## Usando UPnP (Universal Plug and Play)

Si la configuración cambia de %%UPnP|upnp%% a Reenvío de Puertos Manual al recargar, Unraid podría no poder comunicarse con su router. Verifique que %%UPnP|upnp%% esté habilitado y considere actualizar el firmware de su router.

Para configurar %%UPnP|upnp%%:

1. **Habilita %%UPnP|upnp%% en tu router.**
   Asegúrate de que tu router sea compatible con %%UPnP|upnp%% y verifica que esté habilitado en la configuración del router.

2. **Habilita %%UPnP|upnp%% en Unraid.**
   Navega a ***Configuración → Acceso al Management*** y cambia **Usar %%UPnP|upnp%%** a **Sí**.

3. **Selecciona %%UPnP|upnp%% en Unraid Connect.**
   En la página de configuración de la API de Unraid, establece **Allow Remote Access** en **Always**, establece **Remote Access Forward Type** en **UPnP** y luego haz clic en **Apply**.

4. **Verifica el reenvío de puertos.**
   Haz clic en el botón **Check**. Si tiene éxito, verás el mensaje: "Your Unraid Server is reachable from the Internet."

:::caution\[Troubleshooting]

Si la configuración cambia de %%UPnP|upnp%% a Reenvío de Puertos Manual al recargar, Unraid podría no poder comunicarse con su router. Verifique que %%UPnP|upnp%% esté habilitado y considere actualizar el firmware de su router.

:::

---

## Usando reenvío de puerto manual

El reenvío de puerto manual proporciona un mayor control y es compatible con la mayoría de los routers.

El acceso local seguro asegura que todas las conexiones a tu Unraid %%WebGUI|web-gui%%, incluso dentro de tu red doméstica u oficina, se cifren usando HTTPS, protegiendo así cualquier información sensible, como credenciales de inicio de sesión y datos de configuración.

1. **Elige un puerto WAN:** Elige un número de puerto aleatorio por encima de 1000 (por ejemplo, 13856 o 48653), en lugar de usar el puerto por defecto 443.

2. **Aplica configuraciones en Unraid:** Haz clic en **Aplicar** para guardar el puerto que seleccionaste.

3. **Configure su router:** Establezca una regla de reenvío de puertos en su router, direccionando su puerto WAN elegido al puerto HTTPS de su servidor. La interfaz de Unraid proporciona los puertos correctos y la dirección IP.

   Algunos routers pueden requerir que el puerto WAN y el puerto HTTPS coincidan. Si es así, use el mismo número alto y aleatorio para ambos.

4. **Verifica el reenvío de puertos:** Pulsa el botón **Check**. Si todo es correcto, verás “Your Unraid Server is reachable from the Internet.”

5. **Accede a tu servidor:** Inicia sesión en [Unraid Connect](https://connect.myunraid.net/) y haz clic en el enlace **Gestionar** para conectar con tu servidor de forma remota.

---

## Habilitar acceso local seguro

El acceso local seguro asegura que todas las conexiones a tu Unraid %%WebGUI|web-gui%%, incluso dentro de tu red doméstica u oficina, se cifren usando HTTPS, protegiendo así cualquier información sensible, como credenciales de inicio de sesión y datos de configuración.

Beneficios del acceso local seguro incluyen:

- **Cifrado** - Todos los datos intercambiados entre tu navegador y el servidor están protegidos.
- **Consistencia** - Usa la misma URL segura para acceso tanto local como remoto.
- **Cumplimiento** - Cumple con las mejores prácticas de seguridad para proteger interfaces administrativas.

Para habilitar el acceso local seguro:

1. Ve a ***Configuraciones → Acceso de Gestión***.
2. En la sección de **certificado firmado por CA**, verifica las advertencias de Rebobinado DNS.
   - Si no se muestran advertencias, configura **Usar SSL/TLS** a **Estricto**.
   - Si hay advertencias, revisa [Protección Contra Rebobinado DNS](../unraid-os/system-administration/secure-your-server/securing-your-connection.mdx#dns-rebinding-protection).

:::important

Con %%SSL|ssl%%/%%TLS|tls%% configurado en Estricto, los dispositivos clientes deben resolver el nombre DNS de su servidor. Si su conexión a Internet falla, el acceso al %%WebGUI|web-gui%% puede perderse. Consulte [Acceder a su servidor cuando DNS está caído](../unraid-os/system-administration/secure-your-server/securing-your-connection.mdx#accessing-your-server-when-dns-is-down) para pasos de recuperación.

:::
