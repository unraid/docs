1. Si tu servidor está en ejecución y el volumen de arranque está compartido en tu red, abre el recurso compartido **`flash`** (dispositivo de arranque; mismo nombre de recurso compartido en la mayoría de las instalaciones) en **Red**.
2. Arrastre y suelte el archivo de clave de registro en el directorio `config`.
3. En el %%WebGUI|web-gui%%, **Detener** el %%array|array%%, luego **Iniciar** el %%array|array%% de nuevo para aplicar la nueva clave.
