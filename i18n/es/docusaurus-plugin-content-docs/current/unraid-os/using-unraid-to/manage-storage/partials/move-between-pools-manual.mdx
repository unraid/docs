1. **Deshabilitar los servicios de Docker y %%VM|vm%%** como se describió anteriormente.
2. Usar comandos `rsync` o `mv` para transferir archivos entre puntos de montaje (p. ej., `/mnt/pool1/share` a `/mnt/pool2/share`).
3. Verifica los archivos antes de eliminarlos del pool original.
4. Vuelve a habilitar los servicios de Docker y %%VM|vm%%.

:::caution

Siempre revisa tus rutas de archivos y usa los nombres de compartir correctos para evitar la pérdida de datos.

:::
