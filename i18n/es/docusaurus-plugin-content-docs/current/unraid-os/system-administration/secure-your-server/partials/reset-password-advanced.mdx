Este método solo eliminará la **contraseña de%%root|root-user%%** y dejará otros usuarios intactos.

Para restablecer vía este método:

1. **Apaga** tu servidor Unraid.

2. **Conecta el dispositivo USB de arranque** a una computadora (Windows o Mac).

3. **Abre el archivo** ubicado en `/config/shadow` usando un editor de texto (como Notepad++).

4. **Edite la línea** que se ve como:
   `root:$&$&%*1112233484847648DHD$%.:15477:0:99999:7:::`

   a esto en su lugar:

   `root::15477:0:99999:7:::`

5. **Guarda los cambios** en el archivo y vuelve a conectar el dispositivo USB de arranque a tu servidor.

6. **Inicia tu servidor Unraid** y establece una nueva contraseña de root.
